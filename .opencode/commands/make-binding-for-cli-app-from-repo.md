---
description: Given the link to a GitHub repo for a CLI app, set up a new binding package so that it can be used programmatically and typesafely
agent: plan
---

You will be given a link to a GitHub repo which is for a CLI application and your task is to set up a new binding package for it in `bindings/` so that we can use it programmatically and typesafely elsewhere.

- Verify that the CLI application is downloaded and runnable in the current environment by running e.g. `which`. If it's not, download it and verify that it works.

- Look at some of our other bindings in `bindings/` to understand what we want

- Plan the implementation

---

$ARGUMENTS
