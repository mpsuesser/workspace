# Shell worker meta-prompt

You are implementing the managed runtime shell for `subagents-effect`.

Read:

- `prompts/implementation-slices/00-global-rules.md`
- `prompts/implementation-slices/02-managed-runtime-extension-shell.md`
- existing domain files from slice 01

Implement slice 02 only.

Keep the placeholder tool intentionally small. Do not implement real subagent execution.

Before finishing:

- run `npm run check`
- include fake Pi API tests
- document how runtime disposal is verified
