firstchar_A() {
  # Is the current buffer (command line) empty?
  if [[ -z "$BUFFER" ]]; then
    BUFFER="opencode2"
    zle accept-line
  else
    # Behave like a normal 'A'
    LBUFFER+="A"
  fi
}

zle -N firstchar_A
bindkey "A" firstchar_A
