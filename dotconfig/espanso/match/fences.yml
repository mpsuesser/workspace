matches:
  - triggers: [;;-]
    replace: |-
      -------------------------------------------------------------------------------

  - triggers: [;;.]
    replace: /* ... */

  - triggers: [;;`]
    replace: |-
      ```
      $|$
      ```
