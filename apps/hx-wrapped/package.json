{
  "name": "@workspace/hx-wrapped",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --bytecode --minify src/main.ts --outfile $HOME/.local/bin/hx-wrapped",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:",
    "effect": "catalog:"
  }
}
